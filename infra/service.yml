apiVersion: v1
kind: Service
metadata:
  name: person-service
spec:
  selector:
    app: person-service
  type: LoadBalancer
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080 